- hosts: web
  become: true
  vars_files:
    - "{{ inventory_dir }}/group_vars/secrets.yml"
  roles:
    - common
    - wifi
    - web
    - db
    - storage
    - postgres
    - proxy
    - backups
    - monitoring
    - portainer
    - app
